import ForwardRoundedIcon from "@mui/icons-material/ForwardRounded";

export default function ArrowDown({ color }: { color: string }) {
  return <ForwardRoundedIcon sx={{ transform: "rotate(90deg)", color }} />;
}
