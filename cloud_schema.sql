
CREATE TABLE stock (
    stock_id TEXT PRIMARY KEY,
    stock_name TEXT,
    industry_group TEXT,
    market_type TEXT,
    issued_shares BIGINT
);

CREATE TABLE daily_deal (
    stock_id TEXT,
    t DATE,
    c NUMERIC(10,2),
    o NUMERIC(10,2),
    h NUMERIC(10,2),
    l NUMERIC(10,2),
    v BIGINT,
    PRIMARY KEY (stock_id, t)
);

CREATE TABLE daily_skills (
    stock_id TEXT,
    t DATE,
    ma5 NUMERIC(10,2),
    ma5_ded NUMERIC(10,2),
    ma10 NUMERIC(10,2),
    ma10_ded NUMERIC(10,2),
    ma20 NUMERIC(10,2),
    ma20_ded NUMERIC(10,2),
    ma60 NUMERIC(10,2),
    ma60_ded NUMERIC(10,2),
    ma120 NUMERIC(10,2),
    ma120_ded NUMERIC(10,2),
    ma240 NUMERIC(10,2),
    ma240_ded NUMERIC(10,2),
    ma50 NUMERIC(10,2),
    ma50_ded NUMERIC(10,2),
    ema5 NUMERIC(10,2),
    ema10 NUMERIC(10,2),
    ema20 NUMERIC(10,2),
    ema60 NUMERIC(10,2),
    ema120 NUMERIC(10,2),
    macd NUMERIC(10,2),
    dif NUMERIC(10,2),
    osc NUMERIC(10,2),
    k NUMERIC(10,2),
    d NUMERIC(10,2),
    j NUMERIC(10,2),
    rsi5 NUMERIC(10,2),
    rsi10 NUMERIC(10,2),
    bollUb NUMERIC(10,2),
    bollMa NUMERIC(10,2),
    bollLb NUMERIC(10,2),
    obv NUMERIC(10,2),
    obv_ma5 NUMERIC(10,2),
    obv_ma10 NUMERIC(10,2),
    obv_ma20 NUMERIC(10,2),
    obv_ma60 NUMERIC(10,2),
    obv_ema5 NUMERIC(10,2),
    obv_ema10 NUMERIC(10,2),
    obv_ema20 NUMERIC(10,2),
    obv_ema60 NUMERIC(10,2),
    mfi NUMERIC(10,2),
    tenkan NUMERIC(10,2),
    kijun NUMERIC(10,2),
    senkouA NUMERIC(10,2),
    senkouB NUMERIC(10,2),
    chikou NUMERIC(10,2),
    di_plus NUMERIC(10,2),
    di_minus  NUMERIC(10,2),
    adx NUMERIC(10,2),
    cmf NUMERIC(10,2),
    cmf_ema5 NUMERIC(10,2),
    turnover_rate NUMERIC(10,4),
    PRIMARY KEY (stock_id, t)
);

CREATE TABLE weekly_deal (
    stock_id TEXT,
    t DATE,
    c NUMERIC(10,2),
    o NUMERIC(10,2),
    h NUMERIC(10,2),
    l NUMERIC(10,2),
    v BIGINT,
    PRIMARY KEY (stock_id, t)
);

CREATE TABLE weekly_skills (
    stock_id TEXT,
    t DATE,
    ma5 NUMERIC(10,2),
    ma5_ded NUMERIC(10,2),
    ma10 NUMERIC(10,2),
    ma10_ded NUMERIC(10,2),
    ma20 NUMERIC(10,2),
    ma20_ded NUMERIC(10,2),
    ma60 NUMERIC(10,2),
    ma60_ded NUMERIC(10,2),
    ma120 NUMERIC(10,2),
    ma120_ded NUMERIC(10,2),
    ma240 NUMERIC(10,2),
    ma240_ded NUMERIC(10,2),
    ma50 NUMERIC(10,2),
    ma50_ded NUMERIC(10,2),
    ema5 NUMERIC(10,2),
    ema10 NUMERIC(10,2),
    ema20 NUMERIC(10,2),
    ema60 NUMERIC(10,2),
    ema120 NUMERIC(10,2),
    macd NUMERIC(10,2),
    dif NUMERIC(10,2),
    osc NUMERIC(10,2),
    k NUMERIC(10,2),
    d NUMERIC(10,2),
    j NUMERIC(10,2),
    rsi5 NUMERIC(10,2),
    rsi10 NUMERIC(10,2),
    bollUb NUMERIC(10,2),
    bollMa NUMERIC(10,2),
    bollLb NUMERIC(10,2),
    obv NUMERIC(10,2),
    obv_ma5 NUMERIC(10,2),
    obv_ma10 NUMERIC(10,2),
    obv_ma20 NUMERIC(10,2),
    obv_ma60 NUMERIC(10,2),
    obv_ema5 NUMERIC(10,2),
    obv_ema10 NUMERIC(10,2),
    obv_ema20 NUMERIC(10,2),
    obv_ema60 NUMERIC(10,2),
    mfi NUMERIC(10,2),
    tenkan NUMERIC(10,2),
    kijun NUMERIC(10,2),
    senkouA NUMERIC(10,2),
    senkouB NUMERIC(10,2),
    chikou NUMERIC(10,2),
    di_plus NUMERIC(10,2),
    di_minus NUMERIC(10,2),
    adx NUMERIC(10,2),
    cmf NUMERIC(10,2),
    cmf_ema5 NUMERIC(10,2),
    turnover_rate NUMERIC(10,4),
    PRIMARY KEY (stock_id, t)
);

CREATE TABLE hourly_deal (
    stock_id TEXT,
    ts TIMESTAMP WITHOUT TIME ZONE,
    c NUMERIC(10,2),
    o NUMERIC(10,2),
    h NUMERIC(10,2),
    l NUMERIC(10,2),
    v BIGINT,
    PRIMARY KEY (stock_id, ts)
);

CREATE TABLE hourly_skills (
    stock_id TEXT,
    ts TIMESTAMP WITHOUT TIME ZONE,
    ma5 NUMERIC(10,2),
    ma5_ded NUMERIC(10,2),
    ma10 NUMERIC(10,2),
    ma10_ded NUMERIC(10,2),
    ma20 NUMERIC(10,2),
    ma20_ded NUMERIC(10,2),
    ma60 NUMERIC(10,2),
    ma60_ded NUMERIC(10,2),
    ma120 NUMERIC(10,2),
    ma120_ded NUMERIC(10,2),
    ma240 NUMERIC(10,2),
    ma240_ded NUMERIC(10,2),
    ma50 NUMERIC(10,2),
    ma50_ded NUMERIC(10,2),
    ema5 NUMERIC(10,2),
    ema10 NUMERIC(10,2),
    ema20 NUMERIC(10,2),
    ema60 NUMERIC(10,2),
    ema120 NUMERIC(10,2),
    macd NUMERIC(10,2),
    dif NUMERIC(10,2),
    osc NUMERIC(10,2),
    k NUMERIC(10,2),
    d NUMERIC(10,2),
    j NUMERIC(10,2),
    rsi5 NUMERIC(10,2),
    rsi10 NUMERIC(10,2),
    bollUb NUMERIC(10,2),
    bollMa NUMERIC(10,2),
    bollLb NUMERIC(10,2),
    obv NUMERIC(10,2),
    obv_ma5 NUMERIC(10,2),
    obv_ma10 NUMERIC(10,2),
    obv_ma20 NUMERIC(10,2),
    obv_ma60 NUMERIC(10,2),
    obv_ema5 NUMERIC(10,2),
    obv_ema10 NUMERIC(10,2),
    obv_ema20 NUMERIC(10,2),
    obv_ema60 NUMERIC(10,2),
    mfi NUMERIC(10,2),
    tenkan NUMERIC(10,2),
    kijun NUMERIC(10,2),
    senkouA NUMERIC(10,2),
    senkouB NUMERIC(10,2),
    chikou NUMERIC(10,2),
    di_plus NUMERIC(10,2),
    di_minus NUMERIC(10,2),
    adx NUMERIC(10,2),
    cmf NUMERIC(10,2),
    cmf_ema5 NUMERIC(10,2),
    turnover_rate NUMERIC(10,4),
    PRIMARY KEY (stock_id, ts)
);

CREATE TABLE weekly_deal (
    stock_id TEXT,
    t DATE,
    c NUMERIC(10,2),
    o NUMERIC(10,2),
    h NUMERIC(10,2),
    l NUMERIC(10,2),
    v BIGINT,
    PRIMARY KEY (stock_id, t)
);

CREATE TABLE turnover_rank (
    stock_id TEXT NOT NULL,
    stock_name TEXT,
    price NUMERIC,           -- 當日收盤價
    type TEXT,               -- 類型，例如 'red_ball'
    record_date DATE NOT NULL,
    PRIMARY KEY (stock_id, record_date, type)
);